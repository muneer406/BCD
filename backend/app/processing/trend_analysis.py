from typing import Dict, List


def compute_trend(scores: List[float]) -> Dict[str, str]:
    # TODO: Replace with real trend analysis logic
    if not scores:
        return {"overall_trend": "stable"}

    return {"overall_trend": "stable"}
