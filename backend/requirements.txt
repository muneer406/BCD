fastapi==0.110.0
uvicorn==0.27.1
python-dotenv==1.0.1
supabase==2.10.0
pyjwt==2.8.0
requests==2.32.3
pytest==7.4.0
httpx==0.27.0

# Phase 3 ML Dependencies
torch==2.1.0
torchvision==0.16.0
opencv-python-headless==4.8.1.78
pillow==10.1.0
numpy==1.24.3

# Issue #149: secure local encrypted storage
# Native SQLCipher bindings require libsqlcipher-dev; they are optional.
# The fallback in app/services/local_db.py uses cryptography (already required)
# for per-row AES-256-GCM encryption on top of stdlib sqlite3.
pysqlcipher3; sys_platform != "win32"
sqlcipher3; sys_platform != "win32"

# Phase 9: pHash fast pre-check (hybrid approach)
imagehash==4.3.1

# Phase 5: Rate limiting
slowapi==0.1.9
